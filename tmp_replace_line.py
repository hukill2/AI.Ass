from pathlib import Path
path = Path('docs/operator-workflow-wrapper-spec-v1.md')
lines = path.read_text(encoding='utf-8', errors='ignore').splitlines()
lines[30] = ('1. **Preflight stage**: before touching the prompt-template guard, run alidate-execution-candidates-schema-v1.js so the wrapper ensures the candidate ledger exposes the required IDs, statuses, and context the auto-selection logic just relies on, then run alidate-execution-candidates-uniqueness-v1.js so duplicate IDs cannot produce non-deterministic selection, alidate-eligible-candidate-logs-v1.js to confirm the candidate has a successful qwen-readonly log, alidate-eligible-candidate-payloads-v1.js to verify that same candidate already links to an executor payload with a non-empty payload_id, alidate-eligible-candidate-handoff-preview-v1.js to ensure those referenced handoff/preview artifacts exist, alidate-eligible-candidate-review-v1.js to confirm the candidate links to an existing decision review, alidate-eligible-candidate-review-link-v1.js to ensure the linked review has operator_status= approved before readiness continues, alidate-eligible-candidate-review-status-v1.js to block candidates whose linked review reports an explicit rejecting status (e.g., ejected, denied), alidate-eligible-candidate-task-context-v1.js to ensure the candidate\'s 	ask_id points to a recorded task context (otherwise the guard stops the stage before the decision checks), alidate-eligible-candidate-decision-linkage-v1.js to ensure that decision_id matches the linked review, alidate-eligible-candidate-decision-exists-v1.js to confirm that referenced decision record is present, alidate-eligible-candidate-decision-files-v1.js to verify that the same decision exposes a non-empty iles_to_create_or_update, and alidate-eligible-candidate-no-prior-write-v1.js to stop when a prior successful qwen-write already exists. Next, if --execution-id=<id> was provided, run alidate-local-write-readiness-v1.js --execution-id=<id> to confirm the write-lane prerequisites for that execution. After that, run check-prompt-template-mirror-v1.js, followed by alidate-execution-candidate-anomalies-v1.js, alidate-all-review-lanes-state-v1.js, alidate-execution-candidate-coverage-buckets-v1.js, alidate-execution-candidate-view-coverage-v1.js, and alidate-unreviewed-execution-state-v1.js. When the flag is omitted, the wrapper auto-selects a single awaiting/prepared execution candidate (if one exists) for the readiness check; if zero or multiple eligible candidates are present it logs the skip reason and proceeds with the same script order.')
path.write_text('\n'.join(lines), encoding='utf-8')
