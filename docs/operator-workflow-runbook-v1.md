# Operator Workflow Runbook v1

This document is historical context only.

Use `docs/operator-runbook-and-usage-layer-v1.md` as the active operator-facing runbook for `scripts/operator-workflow-wrapper-v1.js`.

## Why this file is no longer canonical
- The wrapper now has one active contract in `docs/operator-workflow-wrapper-spec-v1.md`.
- The wrapper now has one active operator runbook in `docs/operator-runbook-and-usage-layer-v1.md`.
- This older runbook previously drifted from the implemented wrapper output wording and stage-result contract.

## Current operator path
1. Read `docs/operator-workflow-wrapper-spec-v1.md` for the wrapper contract, supported flags, and stage mapping.
2. Read `docs/operator-runbook-and-usage-layer-v1.md` for the active operator workflow, stop conditions, and next actions.

Do not treat this file as current operational truth.
